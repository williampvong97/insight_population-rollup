# insight_population-rollup

# First, specify the input and output .csv filenames, if they are different than given in the instructions.
# The default filenames are 'censustract-00-10.csv' and 'report.csv'
